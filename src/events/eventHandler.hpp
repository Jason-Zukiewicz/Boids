#pragma once

#include <SDL2/SDL.h>
#include <vector>

using std::vector;

class EventHandler
{

public:
    EventHandler();
    ~EventHandler();
    void check();
    // void setObjects(vector<Object> *objs);

private:
};
